import torch
import torch.nn as nn

### Planning

# 1. Download training data from dataset folders
# 2. Run through a transformer model to get embeddings
# 3. Use the embeddings to train a classifier
# 4. Save the classifier model
# 5. Save the embeddings for future use












